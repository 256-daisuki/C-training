                    if (b >= 0) {
                            push(&stack, sqrt(b));
                        } else {
                            printf("エラー：負の数のルートは虚数解になります\n");
                        }